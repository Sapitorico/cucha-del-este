---
import type { HTMLAttributes } from 'astro/types'

interface Props {
  className?: string
}

const { className } = Astro.props
---

<div
  role="group"
  class={`basis-full min-w-0 shrink-0 grow-0 grid  place-items-center  ${className}`}
>
  <slot />
</div>
